{
  "name": "transkriptor-verificacao-web",
  "private": true,
  "version": "0.0.0",
  "description": "Runners locais de teste para a interface e a extensão Meet.",
  "scripts": {
    "test:unit": "vitest run --config tests/js/vitest.config.mjs",
    "test:e2e": "playwright test --config playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "jsdom": "28.0.0",
    "vitest": "5.0.1"
  }
}
