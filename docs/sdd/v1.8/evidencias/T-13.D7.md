# T-13.D7 — resultado nomeado e correção por reunião

- Estado: DONE
- Base: `93a2bf6f1de7760df4121a50da0d563ba95a453b`
- Resultado: `52e7b4edbf5911b2bce706eb2e63e1366c070c42` (commit histórico de fechamento)
- Requisito: `FR-13.D7`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; Node v22.22.0 (vitest + Chromium)
- Arquivos alterados: `resultado_reuniao.py`, `resultado_edicao.py` (novo, extração p/ limite), `renomear_falante_flow.py`, `transkriptor_menu_flows.py`, `assistente.py`, `templates/assistente.html`, `static/assistente.js`, `tests/test_resultado_reuniao.py` (novo), `tests/e2e/participants.spec.js` (novo), `docs/sdd/v1.8/evidencias/T-13.D7.md` (este)
- Decisões locais: JSON canônico + TXT derivado exato `[HH:MM:SS] Nome: texto`; sem atribuição → literal `Identificação pendente` (nunca `FALANTE_XX`); correção exige `expected_revision` (409 em conflito), gera revisão imutável + undo, sem biometria (voz só em `persistir_renomeacao_falante`); `resultado_global` nunca apresenta parcial como completo; endpoints sob token existente + path confinado; drawer com foco restaurado e `escapeHtml`; skills ausentes — ciclo literal.

## Baseline

`test_renomear_falante_flow` → 4 aprovados (só enrollment biométrico). Resultado estruturado não existia.

## RED

Esqueleto validou a ausência; incidente de sobrescrita de `resultado_reuniao.py` contido por reconstrução verificada (diff só aditivo + 23 testes legados verdes). Comportamento novo coberto pelos 7 casos D7.

## GREEN

`test_resultado_reuniao + test_renomear_falante_flow` → **13 passed** (formato exato, pendente sem invenção, correção pós-restart, isolamento entre reuniões, concorrência recusada, undo, diarização parcial, API lista/corrige/desfaz/404).

## Regressão e gate

- Python completa: **619 passed**; unit JS **12 passed**; e2e **7 passed** (participants: lista/corrige/desfaz/foco + 409 sem perder drawer).
- `compileall` + `diff-check` → exit 0; limites OK (reuniao 331, edicao 221, flows 134/467, assistente 357).
- Gate F13.D completo (Chrome+Edge, 3 pessoas, 2 abas, homônimos): **PENDENTE** — reunião consentida com participantes (DU-12).

## Segurança e dados

Correção valida nome (≤80, sem id interno); meeting_id com padrão + confinamento; nomes sintéticos; sem biometria implícita; sem áudio/conta real.

## Limitações

D7 DONE (automatizado). F13.D fecha no gate físico com participantes. Próxima: T-13.E1 (política de proteção).
