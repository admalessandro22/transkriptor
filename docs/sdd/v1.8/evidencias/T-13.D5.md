# T-13.D5 — entregar os eventos ao worker

- Estado: DONE
- Base: `af15c998030d7608bc8c6d0aa1a717704863b967`
- Resultado: commit de fechamento deste arquivo
- Requisito: `FR-13.D5`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1
- Arquivos alterados: `fila_processamento.py`, `job_v2.py` (novo), `processador_reuniao.py`, `retranscritor.py`, `app_processamento.py`, `eventos_meet_store.py` (`ancora_refs`), `app_ciclo_reuniao.py` (âncora), `tests/test_nomes_meet_worker.py` (novo), `docs/sdd/v1.8/evidencias/T-13.D5.md` (este)
- Decisões locais: snapshot `sessao` + `eventos_refs` + `preferencias` (rótulo/vozes congelados) no job v2, opcionais (v1 lê com defaults); refs validadas no enqueue e revalidadas (hash) no worker; ref adulterada → aviso + segue sem nomes (nunca fabrica); `ancora_refs` ancora refs na raiz de transcrições; validadores em `job_v2.py` (fila 490 linhas, limite 500); `diarizacao_final.py` inalterado (já recebia `eventos_meet`); skills ausentes — ciclo literal.

## Baseline

`test_processador_reuniao + test_retranscritor + test_fila_processamento` → **19 aprovados**. Job sem sessão/refs.

## RED

`tests/test_nomes_meet_worker.py` → 3 falhas (`enfileirar() got unexpected keyword 'sessao'`); v1-sem-nome já passava.

## GREEN

→ exit code 0, **4 aprovados** (Ana/Bruno atravessam refs até `diarizar`; restart com nova `Fila` preserva e conclui; hash adulterado recusado com aviso e `eventos_meet == []`; v1 resulta em `FALANTE`, sem nome inventado). Dois ajustes de harness honestos: store do teste sob a raiz da fila (como o app); modelo fake em chamada única.

## Regressão e gate

- Teste final D5: nomes + processador + retranscritor + fila → **23 aprovados**.
- Fila/concorrência/limites: **30 aprovados** (inclui limite 500 após extração `job_v2`).
- Suíte completa: **604 passed**; `compileall` + `diff-check` → exit 0.
- Gate real: não exigido em D5 (Ana/Bruno sintéticos; nome verificado no segmento via spy, não só argumento de mock).

## Segurança e dados

Refs = caminhos relativos + hash (sem conteúdo no job); descriptografia só no worker; nomes sintéticos. Nada real tocado.

## Limitações

D5 DONE. Próxima: T-13.D6 (atribuição conservadora calibrada).
