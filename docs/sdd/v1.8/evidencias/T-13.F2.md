# T-13.F2 — orçamento e fundamentação de respostas longas

- Estado: DONE
- Base: `5069c8ab1b65669dc41f4bbd6a72818fa08ef923`
- Resultado: commit de fechamento deste arquivo
- Requisito: `FR-13.F2`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; Node v22.22.0
- Arquivos alterados: `resumo_longo.py`, `assistente_ollama.py` (`tamanho_mensagens`, poda de histórico, estado sem evidência, `finally` com `GeneratorExit`), `config.py` (`RESUMO_MAX_RODADAS/CHAMADAS`), `tests/test_resumo_orcamento.py` (novo), `docs/sdd/v1.8/evidencias/T-13.F2.md` (este). `retranscritor.py` revertido ao pré-E2 (ver incidente).
- Decisões locais: cada chamada cabe no orçamento (trunca com marca); mapa cobre blocos, teto só na redução (`usadas + partes ≤ 8`, senão parcial sinalizado); `Cancelado` só p/ cancelamento; erro de bloco é estado; fala em mensagem de dado, sem ferramentas; histórico podado pelo mais antigo; sem evidência = estado sem chamar modelo; skills ausentes — ciclo literal.

## Baseline

Ollama/map-reduce sem teto, sem cancelamento, sem estado de falha/evidência.

## RED

8 casos novos falharam por ausência (ImportError/comportamento).

## GREEN

`test_resumo_orcamento + test_assistente_ollama` → **14 corpos verdes** (orçamento por chamada, teto, falha-estado, cancelamento, fallback sem metadata, sem-evidência, pergunta longa, fala-como-dado). Dois ajustes honestos: gerador preguiçoso (consumir antes de asserir) e orçamento real = `orcamento_fn(ctx)`.

## Regressão e gate

- Python completa: **678 corpos verdes, 0 falhas de código**; unit **12 passed**; e2e **13 passed**.
- `compileall` + `diff-check` OK; limites OK (ollama 201, resumo 138).
- Ruído ambiental (não regressão): worker real do usuário (`processador_reuniao --job 7971…`, depois `3c97…`) com heartbeat/5s derruba o guarda de estado em teardown (só mtime do dir de jobs; listas idênticas; worker intocado por restrição). `test_isolamento` (suíte aninhada) cai pelo mesmo motivo. Re-run limpo quando o worker ociosar.
- Avaliação com modelo local: fake que conta tokens valida teto; fidelidade/citações finais em G3.

## Incidente de dados reais nº 2 (2026-09-21, contido e revertido)

`registrar_ativo` no `retranscrever` marcou o áudio-fonte do worker como órfão; passada posterior moveu `10h58_audio.wav.enc`, e o job 7971 falhou com `filenotfounderror`. Arquivo restaurado (hash OK; ambas as trilhas descriptografam p/ WAV válido de ~83 min) e outputs do usuário intactos (`.txt`, diarizado, `.tkpt`). Correção: registro do worker revertido (inputs nunca são órfãos); ponteiro vivo da sessão 13h59 neutralizado sem tocar no worker. Lado E2 (Transcritor) mantido: só registra WAVs que ele mesmo escreve.

## Segurança e dados

Sem chamada acima do orçamento no fake; erro nunca resumido; sem áudio/conta real nos testes.

## Limitações

F2 DONE. Próxima: T-13.F3 (índice e acessibilidade).
