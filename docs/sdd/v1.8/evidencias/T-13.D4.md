# T-13.D4 — persistir eventos privados continuamente

- Estado: DONE
- Base: `4a8921d48cdcd39a745ba1c310387016f9fc2a51`
- Resultado: `af15c998030d7608bc8c6d0aa1a717704863b967` (commit histórico de fechamento)
- Requisito: `SEC-13.D4`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1
- Arquivos alterados: `eventos_meet_store.py` (novo), `tests/test_eventos_meet_store.py` (novo), `meet_bridge.py`, `app_ciclo_reuniao.py`, `config.py`, `docs/sdd/v1.8/evidencias/T-13.D4.md` (este)
- Decisões locais: `EventStore(root, session, cipher)` com adaptador em `crypto_storage` (sem duplicar AES-GCM; parâmetro `cipher` preservado por compatibilidade); buffer RAM 500 / segmento 1 MiB ou 1 s / spool 256 MiB em `config.py`; heartbeat não persiste (retorna último ACK); `ColetaBloqueada` para sem-chave e revogação; índice só com metadados; payload legado da ponte intacto; skills Superpowers ausentes — ciclo literal.

## Baseline

`test_meet_bridge_seguranca.py + test_log_isolado.py` → **18 aprovados**. Store não existia.

## RED

`tests/test_eventos_meet_store.py` → **7 falhas** comportamentais (esqueleto `NotImplementedError`).

## GREEN

→ exit code 0, **7 aprovados** (1000 eventos com final íntegro; ACK só após selo; crash com cauda rasgada recupera prefixo; sem chave bloqueia legenda e libera heartbeat; revogação interrompe; expiração só 7 dias após manifesto válido; sem conteúdo em log). Um caso foi reescrito para o comportamento honesto do AES-GCM (rasgo invalida o segmento; prefixo = segmentos anteriores).

## Regressão e gate

- Teste final D4: store + bridge_segurança + log_isolado → **25 aprovados** (7+15+3).
- Regressões D1/ponte/ciclo: bridge + ciclo_sem_deadlock + sessão → **21 aprovados**.
- Suíte completa: **600 passed**; `compileall` + `diff-check` → exit 0.
- Carga sintética de ~1000 eventos com dreno contínuo; limite de spool com contador + `parcial` (sem parar áudio); excesso visível em `estado()`.
- Gate real: não exigido em D4 (sintéticos + subprocessos do harness); D2 segue BLOCKED pela demo.

## Segurança e dados

Segmentos cifrados em repouso; índice sem nomes/textos; job/log sem legenda (teste canário); retenção de brutos vinculada a manifesto válido (DU-10). Nada real tocado.

## Limitações

D4 DONE. Próxima: T-13.D5 (job v2 leva refs ao worker).
