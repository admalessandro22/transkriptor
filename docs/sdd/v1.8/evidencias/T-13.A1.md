# T-13.A1 — reconciliar a fonte SDD

- Estado: `DONE`
- Base: `eff83a61f3ee2291e48a80a00da3f70f1475ccbc`
- Resultado: `0b7a11dbaaa26d3701c2979d5dff9d613619e0c1` (commit de implementação)
- Requisito: `NFR-13.A1`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1
- Arquivos alterados: `AGENTS.md`, índice `docs/sdd/README.md`, documentação
  ativa v1.8, marcadores históricos v1.6/`VERIFICACAO.md`,
  `scripts/verificar_fase.py`, `tests/test_sdd_rastreabilidade.py` e
  `tests/test_versao.py`.
- Decisões locais: nenhuma. A versão SDD ativa mudou para v1.8; a versão do
  executável continua em `config.VERSAO = 1.7.0`.

## Baseline

`python -m pytest tests/test_versao.py -q --tb=short` → exit code 0,
5 testes aprovados antes da alteração.

## RED

`python -m pytest tests/test_sdd_rastreabilidade.py -v --tb=short` → exit
code 1, 3 falhas: não existia índice ativo e
`verificar_fase.py` não reconhecia `--sdd-root`/a fase `v1.8-sdd`, portanto
não conseguia acusar requisito sem tarefa.

O caso adicional de seletor ausente também ficou vermelho antes de sua
implementação: o comando não reconhecia `--tarefa`. Durante a verificação foi
detectada uma sombra de variável no verificador: o loop de mapeamentos
substituía o parâmetro da tarefa pelo último ID (`T-13.H3`). O diagnóstico foi
registrado e a variável local foi renomeada antes do novo GREEN.

## GREEN

`python -m pytest tests/test_sdd_rastreabilidade.py tests/test_versao.py -v
--tb=short` → exit code 0, 9 testes aprovados.

`python scripts/verificar_fase.py --fase v1.8-sdd --tarefa T-13.A1` → exit
code 0, `RASTREABILIDADE OK`; o comando valida arquivos normativos, links do
índice, requisito→tarefa e a existência dos seletores pytest da tarefa em
execução.

## Regressão e gate

- `git diff --check` → exit code 0 antes do commit de implementação.
- `python -m pytest tests/test_sdd_rastreabilidade.py tests/test_versao.py -v
  --tb=short` → exit code 0, 9 aprovados.
- Nenhum gate físico, reunião real, captura de áudio, OAuth ou extensão foi
  executado nesta tarefa.

## Segurança e dados

Os testes usam somente cópias temporárias da documentação. Nenhum áudio,
transcrição, nome, token, credencial ou perfil de voz foi aberto, gerado ou
registrado. A busca de padrões de credencial nos artefatos novos não encontrou
valor correspondente.

## Limitações

Esta tarefa entrega governança e o gate documental; não altera captura,
transcrição, bridge Meet ou identificação de pessoas. A próxima tarefa
elegível é `T-13.A2`; seus testes JavaScript e de fala sintética ainda não
existem. Gates que usam áudio real continuam pendentes de autorização
específica.
