# T-13.B3 — progresso, cancelamento e recuperação do worker

- Estado: `DONE`
- Base: `86979bd2e4098e5d0780e376fa3b92791c3b390d`
- Resultado: `c9a72555018d05b8711f42f95529d70e99985696`
- Requisito: `FR-13.B3`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; processos Python em modo `spawn`.
- Arquivos alterados: `config.py`, `worker_liveness.py`, `fila_processamento.py`,
  `processador_reuniao.py`, `app_processamento.py`, `docs/sdd/v1.8/interfaces.md`,
  `tests/test_worker_liveness.py`, `tests/alvos_worker_liveness.py`,
  `tests/test_worker_observabilidade.py`, `tests/test_fluxo_reuniao_v15.py`.
- Decisões locais: `WORKER_MODELO_INIT_SEG = 600` (orçamento próprio, não
  compartilhado com a etapa de transcrição); `WORKER_CANCEL_FORCAR_SEG = 30`;
  módulo `worker_liveness.py` para caber no limite de 500 linhas; contratos
  públicos emendados em `interfaces.md`; saída pré-claim conta como tentativa
  limitada e libera a fila.

## Baseline

`python -m pytest tests/test_worker_observabilidade.py tests/test_processador_reuniao.py -v --tb=short` → exit code 0, **7 aprovados**.

## RED

`python -m pytest tests/test_worker_liveness.py -v --tb=short` → exit code 1.

- `test_worker_vivo_sem_progresso_libera_fila`: `wait()` ilimitado, waiter vivo.
- `test_retry_tem_limite`: job sem `attempt`, uma falha e não retenta.
- `test_falha_import_antes_do_claim_nao_reinicia_infinito`: job permanece
  `pending` e a fila não avança.
- Constantes/progresso/cancelamento ausentes na interface antiga; o RED
  comportamental dos três casos acima não foi um `ImportError` do produto.

## GREEN

`python -m pytest tests/test_worker_liveness.py::test_worker_vivo_sem_progresso_libera_fila tests/test_worker_liveness.py::test_cancelar_preserva_audio tests/test_worker_liveness.py::test_retry_tem_limite tests/test_worker_liveness.py::test_progresso_real_renova_deadline tests/test_worker_liveness.py::test_falha_import_antes_do_claim_nao_reinicia_infinito -v --tb=short` → exit code 0, **5 aprovados**.

## Regressão e gate

- Teste final: `python -m pytest tests/test_worker_liveness.py tests/test_worker_observabilidade.py tests/test_processador_reuniao.py -v --tb=short` → exit code 0, **13 aprovados**.
- Gate F13.B: `python -m pytest tests/test_fila_concorrencia.py tests/test_retencao_resultado.py tests/test_worker_liveness.py -v --tb=short` → exit code 0, **21 aprovados**.
- `python -m pytest tests/ -q --tb=short` → exit code 0, **554 aprovados** em 103,39 s.
- `python -m compileall -q` nos `.py` tocados → exit code 0.
- `git diff --check` → exit code 0 (aviso LF/CRLF só em `interfaces.md`).
- Gates físicos 25 s / 600 s: **PENDENTE** (sem autorização DU-12).

## Segurança e dados

Testes usaram `tmp_path`, WAV sintético mínimo e subprocessos do harness.
Encerramento forçado só do PID com lease validado desses subprocessos. Nenhum
áudio real, reunião, conta Google ou arquivo de produção foi usado ou removido.
Locks órfãos acidentais em `.jobs_processamento/.locks` sem JSON correspondente
foram removidos; nenhum job/áudio de produção foi apagado.

## Limitações

Heartbeat contínuo, timeout de inatividade, retry (2) e cancelamento cooperativo
estão no worker/supervisor. A captura por fonte (C1) ainda não começou.

## Correção — progresso por bloco Whisper

O loop de `retranscrever` não chamava `on_status` entre chunks; só o heartbeat
de lease e uma marca inicial de `transcribe` existiam. Uma reunião longa
cairia no timeout de 600 s como worker vivo sem progresso.

- RED: `test_processar_job_avanca_unidades_durante_chunks_whisper` dirigiu
  `processar_job` + `retranscrever` real (WAV de 50 s, dois blocos de 25 s).
  `modelo.transcribe` viu `progress_units == 1` nos dois blocos (`assert 1 > 1`).
- GREEN: `on_status(f"transcribe:{inicio_frame}")` antes de cada
  `modelo.transcribe`; `_on_status` grava `stage=transcribe` nesse prefixo.
  O mesmo seletor passou; unidades cresceram entre chunks.
- `python -m pytest tests/test_worker_liveness.py tests/test_worker_observabilidade.py tests/test_processador_reuniao.py tests/test_retranscritor.py tests/test_limite_linhas.py -v --tb=short` → exit 0, **22 aprovados**.
- Gate F13.B → exit 0, **22 aprovados**.
- `python -m pytest tests/ -q --tb=short` → exit 0, **555 aprovados** em 75,92 s.
