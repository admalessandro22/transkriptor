# T-13.C3 — áudio longo e formatos explícitos

- Estado: DONE
- Base: `e1f4de1770290249cb437d4ba3b834da94dd1798`
- Resultado: `4e943fbe2c25e47304c518dce928044c74736dd5` (commit histórico de fechamento)
- Requisito: `NFR-13.C3`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1
- Arquivos alterados: `audio_reader.py`, `audio_fontes.py`, `retranscritor.py`, `tests/test_audio_streaming.py` (novo), `tests/test_audio_duas_fontes.py` (adaptação mínima do fake), `docs/adr/0001-audio-cifrado-em-blocos.md` (novo), `docs/sdd/v1.8/evidencias/T-13.C3.md` (este)
- Decisões locais: `iter_audio_16k` no reader (origem temporal na taxa real do arquivo, saída 16 kHz por bloco); `retranscrever` usa `transcrever_fonte` também sem mic (fim do `readframes(total)`); WAV temporário da diarização em streaming; `.enc` legado com limite explícito; TKAS/1 adiado a E1 por ADR; fake de C2 ganhou `**_kwargs`/`on_status` para acompanhar a interface.

## Baseline

`python -m pytest tests/test_audio_utils.py tests/test_retranscritor.py tests/test_crypto_storage.py -q --tb=short` → exit code 0, **23 aprovados**. `tests/test_audio_streaming.py` e o ADR não existiam.

## RED

`python -m pytest tests/test_audio_duas_fontes.py tests/test_audio_streaming.py -v --tb=short` → 1 falha comportamental: `test_audio_duas_horas_nao_le_completo` (`readframes` única de 960000 frames via `_ler_audio_pcm`); PCM24/RIFF-extra/limite-`.enc` já valiam como travas (herança de C2).

## GREEN

`python -m pytest tests/test_audio_streaming.py -q --tb=short` → exit code 0, **4 aprovados**, após streaming por blocos ≤30 s, resampling por bloco e WAV-temp em streaming.

## Regressão e gate

- Teste final C3: `python -m pytest tests/test_audio_streaming.py tests/test_audio_utils.py tests/test_retranscritor.py tests/test_crypto_storage.py -q --tb=short` → exit code 0, **27 aprovados** (4+4+5+14).
- Regressão C2+B3: C2 (6), mic/diarização (12) e `test_worker_liveness.py` (7) verdes; uma regressão real apareceu (`test_processar_job_avanca_unidades_durante_chunks_whisper`: progresso por bloco perdido no refactor) — corrigida via `on_status(f"transcribe:{start_frame}")` por bloco em `transcrever_fonte`, mesmo gate re-rodado verde. Nenhum `expected` alterado.
- Suíte completa: `python -m pytest tests/ -qq --tb=short` → exit code 0, **576 passed**.
- `python -m compileall -q audio_reader.py audio_fontes.py retranscritor.py config.py` → exit code 0; `git diff --check` → exit code 0.
- Benchmark (modelo mock, WAV 60 s/1,9 MB): pico `tracemalloc` 6,24 MB independente da duração (bloco 30 s); Whisper real domina tempo/RAM à parte.
- Roundtrip cifrado/legado: `test_retranscrever_wav_enc` + `test_mic_cifrado_aceito` verdes.
- Gate físico 600 s (encerra F13.C): **APROVADO em C1** (9.600.000 frames, 0 falhas) — sem nova captura; F13.C fecha aqui.

## Segurança e dados

WAVs sintéticos (`tmp_path`), modelo fake, `chave_teste`; nenhum áudio real ou dado de produção. Sem segredos nos diffs.

## Limitações

C3 DONE; F13.C encerrada. Próxima: T-13.D1.
