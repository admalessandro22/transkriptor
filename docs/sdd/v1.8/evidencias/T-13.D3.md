# T-13.D3 — roster e parser de legendas confiável

- Estado: DONE
- Base: `765ef1d8beb15fa6f3059037743a57be7aab8d74`
- Resultado: commit de fechamento deste arquivo
- Requisito: `FR-13.D3`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; Node v22.22.0 (vitest + Playwright/Chromium)
- Arquivos alterados: `extension/meet/parser.js` (novo), `tests/js/meet-parser.test.js` (novo), `tests/js/fixtures/meet-*-v1.html` (novas), `extension/meet/content.js`, `extension/meet/manifest.json`, `extension/meet/README.md`, `tests/js/content.test.js`, `tests/e2e/meet-transport.spec.js`, `tests/e2e/content-source.spec.js`, `docs/sdd/v1.8/evidencias/T-13.D3.md` (este)
- Decisões locais: parser em script clássico com guarda `module.exports` (mesmo padrão de `background.js`; sem bundler novo); `content.js` delega extração ao `MeetParser` (camadas movidas, sem duplicar); testes carregam `parser.js` antes (ordem do manifest); nenhum espelho Python criado — regressão Python existente (`test_extensao_parsing.py`) preservada como protocolo.

## Baseline

Parser não existia; extração duplicada dentro de `content.js`. Infra JS verde.

## RED

`tests/js/meet-parser.test.js` → **7 falhas** (`nao implementado`, módulo importável — sem `ImportError`).

## GREEN

`npm run test:unit -- tests/js/meet-parser.test.js` → **7 passed** (legenda com id/revisão; tile silencioso no roster; homônimos com ids distintos; revisão maior vence + repetição colapsa; sem seletores → capacidade indisponível; legenda desligada; câmera desligada via legenda).

## Regressão e gate

- Teste final: `npm run test:unit` → **12 passed**; `npm run test:e2e` → **5 passed** (harness legado adaptado ao canal `chrome.runtime`, mesma asserção; asserção de manifesto atualizada para `["parser.js", "content.js"]`).
- Regressão Python de normalização: `test_extensao_parsing.py + test_meet_bridge_seguranca.py` → **22 passed**.
- Suíte Python completa: **593 passed**; `git diff --check` → exit 0.
- Fixtures executam o JS entregue (`parser.js` real via `require` no vitest e via `addScriptTag` no Chromium).
- Gate real (Meet com 3 pessoas/2 abas/reconexão): **PENDENTE** — exige reunião consentida com participantes (DU-12), autorização própria; D2 segue BLOCKED pelo mesmo motivo.

## Segurança e dados

Fixtures 100% sintéticas (`Pessoa/ Ana Sintética`); nenhum DOM, nome ou conta real. Sem segredos no diff.

## Limitações

D3 DONE (automatizado + fixtures). D2 continua BLOCKED pela demo Chrome+Edge. Próxima: T-13.D4 (spool cifrado; sem tocar coleta real sem autorização).
