# T-13.C2 — transcrever microfone e loopback

- Estado: BLOCKED
- Base: `72cb43f21f9ade8315e0d7dfc499c4d4d1c6df29`
- Resultado: commit deste arquivo (parte automatizada; gate de mic humano pendente)
- Requisito: `FR-13.C2`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1
- Arquivos alterados: `audio_reader.py` (novo), `audio_fontes.py` (novo), `retranscritor.py`, `config.py`, `tests/test_audio_duas_fontes.py` (novo), `docs/sdd/v1.8/evidencias/T-13.C2.md` (este)
- Decisões locais: `AUDIO_BLOCO_MAX_SEG = 30.0`, `AUDIO_CIFRADO_LEGADO_MAX_BYTES = 256 MiB`, `ECO_SIMILARIDADE_MIN = 0.5` em `config.py`; `.wav.enc` decifrado via `crypto_storage.ler_bytes_arquivo` antes de `wave.open` (nunca sobre ciphertext); eco exige sobreposição temporal + Jaccard ≥ 0,5, em dúvida preserva com `overlap=True`; skills Superpowers ausentes — ciclo literal de `executor-llm.md`.

## Baseline

`python -m pytest tests/test_captura_mic.py tests/test_diarizacao_voce.py tests/test_retranscritor.py -q --tb=short` → exit code 0, **17 aprovados**. `tests/test_audio_duas_fontes.py` não existia.

## RED

`python -m pytest tests/test_audio_duas_fontes.py -v --tb=short` → exit code 1, **6 falhas** comportamentais (sem `ImportError`): `test_frase_exclusiva_mic_aparece` (só loopback no TXT), `test_mic_cifrado_aceito`, `test_eco_nao_duplica_frase`, `test_falas_simultaneas_preservadas`, `test_offset_de_inicio_alinha_fontes`, `test_formato_nao_implementado_rejeitado` (esqueletos com `NotImplementedError`).

## GREEN

`python -m pytest tests/test_audio_duas_fontes.py -v --tb=short` → exit code 0, **6 aprovados**, após `audio_reader.py` (PCM 16/24/32 explícito, 8-bit rejeitado, `.enc` via API tipada com limite), `audio_fontes.py` (`transcrever_fonte` por fonte com origem temporal explícita; `mesclar_segmentos` cronológica com dedup só de eco confirmado) e integração em `retranscrever` (com `caminho_mic`, STT das duas fontes + fusão; sem mic, caminho legado intacto).

## Regressão e gate

- Teste final C2: `python -m pytest tests/test_audio_duas_fontes.py tests/test_captura_mic.py tests/test_diarizacao_voce.py tests/test_retranscritor.py -v --tb=short` → exit code 0, **23 aprovados**.
- `python -m pytest tests/test_limite_linhas.py tests/test_processador_reuniao.py -q --tb=short` → exit code 0, **7 aprovados**.
- Suíte completa: `python -m pytest tests/ -qq --tb=short` → exit code 0, **572 passed**.
- `python -m compileall -q audio_reader.py audio_fontes.py retranscritor.py config.py` → exit code 0.
- `git diff --check` → exit code 0.
- Gate de frase humana exclusiva no mic (DU-12, voz real consentida): **PENDENTE — NÃO EXECUTADO**. Exige autorização específica; não substituído por energia RMS nem TTS.

## Segurança e dados

Testes com WAV sintético (`tmp_path`), modelo Whisper fake e `chave_teste`; nenhum áudio real, reunião, conta ou dado de produção. Busca de segredos nos novos `.py`: nenhum achado.

## Limitações

Parte automatizada DONE; C2 como um todo fica BLOCKED exclusivamente pelo gate de mic humano. Checkbox de C2 permanece desmarcada; C3 não começa até o gate ou exceção documental do usuário.
