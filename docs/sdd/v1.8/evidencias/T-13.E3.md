# T-13.E3 — contratos e limites da API local

- Estado: DONE
- Base: `1d9587ed1a04fd7b49d8f3b402e9b560b682ccb2`
- Resultado: commit de fechamento deste arquivo
- Requisito: `SEC-13.E3`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; Node v22.22.0 (vitest + Chromium)
- Arquivos alterados: `assistente_validacao.py` (novo), `assistente.py`, `assistente_ollama.py`, `config.py`, `tests/test_assistente_validacao.py` (novo), `tests/test_assistente_seguranca.py`, `tests/e2e/chat-cancel.spec.js` (novo), `docs/sdd/v1.8/evidencias/T-13.E3.md` (este)
- Decisões locais: payload objeto com strings limitadas e histórico de objetos `user|assistant` (F1 estende p/ `ChatRequest`); Host loopback obrigatório (anti-rebinding) + Origin externa 403; `MAX_CONTENT_LENGTH` no framework + 413 JSON; concorrência 2 com 429; `no-store`/`no-referrer`/nosniff/DENY/CSP; cancel fecha upstream (`finally` + `GeneratorExit`); traversal barrado no payload (400) e no acesso (403); skills ausentes — ciclo literal.

## Baseline

Segurança/token/api → 27 aprovados. Sem contrato tipado nem limites efetivos.

## RED

6 falhas no validador novo + 2 ajustes honestos (traversal agora 400 no payload; `Response.response` é gerador no teste de cancel).

## GREEN

Validação 6/6; HTTP 41/41 (segurança+token+api); e2e cancel passa.

## Regressão e gate

- Teste final E3: validação + segurança + token + api → **41 passed**.
- Python completa: **656 passed**; unit **12 passed**; e2e **8 passed**.
- `compileall` + `diff-check` → exit 0; limites OK (assistente 391).
- Erros previstos 4xx/429, nunca 500 (fuzz sem eco de segredo); sem sockets esquecidos.
- Gate real: não exigido (Ollama mockado; e2e com fetch stubado).

## Segurança e dados

Token nunca ecoado; sem conteúdo em erro/log; tudo em 127.0.0.1. Nada real tocado.

## Limitações

E3 DONE. Próxima: T-13.E4 (logs tipados e ciclo de biometria).
