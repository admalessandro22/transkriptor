# T-13.G1 — dependências reproduzíveis e CI

- Estado: DONE
- Base: `62abbc0ba82014f001215bedea8b951996182e34`
- Resultado: commit de fechamento deste arquivo
- Requisito: `NFR-13.G1`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; CUDA 12.8 (torch 2.11.0+cu128); Node v22.22.0
- Arquivos alterados: `requirements/constraints-cpu.txt` + `constraints-cuda.txt` (novos), `scripts/instalar_helper.py` (`combinacao_valida`, `--check deps --rota`), `instalar.bat` (trava por rota), `.github/workflows/tests.yml` (novo), `docs/DEPENDENCIAS.md` + `docs/sbom.json` (novos), `tests/test_dependencias.py` (novo), `docs/sdd/v1.8/evidencias/T-13.G1.md` (este)
- Decisões locais: núcleo idêntico nas rotas, só torch diverge; SBOM via `importlib.metadata` (sem credenciais); CVE sem advisory aberto no conjunto (revalidar por release); CI só CPU hospedada (CUDA provada localmente); `verificar_fase` fora do CI (gate manual G3); skills ausentes — ciclo literal.

## Baseline

Sem travas, sem pré-checagem de rota, sem CI/SBOM.

## RED

4 ausências (locks, validador, CLI, SBOM).

## GREEN

`test_dependencias + test_instalador_helper` → 9/9; `--check deps` OK nas 2 rotas; `pip check` limpo no conjunto do produto.

## Regressão e gate

- Python completa: **687 corpos verdes, 0 falhas de código**; unit **12 passed**; e2e **16 passed**.
- `pip check`: só acusa `pdfplumber` global alheio ao produto (intocado por regra; sem falso sucesso — conjunto próprio limpo).
- CUDA provada localmente (`cuda: True 12.8`, suíte verde); CPU via travas + CI.
- Ruído ambiental: worker real com heartbeat/5s derruba o guarda em teardown (só mtime; intocado); `test_progresso_real_renova_deadline` flaky sob carga (ENOSPC/WinError 5 isolados, 3/3 verde em seguida).
- Gate físico: separado, sem simulação no CI.

## Segurança e dados

SBOM sem segredos; sem instalação global pelo agente (só PyNaCl autorizado em E1); nada real tocado.

## Limitações

G1 DONE. Próxima: T-13.G2 (instalação e versão coerentes).
