# T-13.E1 — política única de proteção e falha explícita

- Estado: DONE
- Base: `52e7b4edbf5911b2bce706eb2e63e1366c070c42`
- Resultado: commit de fechamento deste arquivo
- Requisito: `SEC-13.E1`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; PyNaCl 1.6.2 (instalação autorizada)
- Arquivos alterados: `politica_privacidade.py` (novo), `crypto_stream.py` (novo), `tests/test_politica_privacidade.py` + `tests/test_crypto_stream.py` (novos), `requirements.txt`, `crypto_storage.py`, `identificador_voz.py`, `perfil_voz_flow.py`, `audio_reader.py`, `retranscritor.py`, `app_bandeja_menu.py`, `docs/sdd/v1.8/evidencias/T-13.E1.md` (este)
- Decisões locais: instalação nova (sem config) → `protected`, existente sem campo → `compatible`; biometria em protegido sem chave falha fechado (`ProtectionUnavailable`, sem plaintext); falha de cifra move p/ `restrito/` (`PROTECTION_PENDING`, única cópia preservada); TKAS/1 exatamente como `interfaces.md` (HKDF-SHA256, AD 9B+u64, FINAL/MESSAGE) sobre bindings PyNaCl; `.tks` no reader com parse RIFF em streaming (pico O(chunk)); migração só inventário + lista exata confirmada (dry-run padrão); menu exibe modo efetivo; skills ausentes — ciclo literal.

## Baseline

`test_crypto_storage + test_identificador_voz + test_transcricao_crypto` → **22 aprovados**. PyNaCl ausente (instalação autorizada em sessão).

## RED

16 falhas: 14 `NotImplementedError`/ausência + regressão real (biometria salvava em claro sem chave) + 2 ajustes de harness (AD após flush; chave do reader = mestra).

## GREEN

→ exit code 0, **16 aprovados** (modos, fail-closed, blob corrompido preservado, área restrita, sem rebaixamento, dry-run; roundtrips, truncado/alterado/reordenado/duplicado/sem-final/chave-errada, `.tks` no reader).

## Regressão e gate

- Teste final E1: política + stream + storage + voz + transcricao_crypto → **38 aprovados**.
- Suíte completa: **635 passed**; `compileall` + `diff-check` → exit 0; limites OK.
- Zero fallback silencioso; compatível segue entregando TXT; nenhuma migração de dado real (só inventário em `tmp_path`).
- Gate real: não exigido em E1 (sintéticos; PyNaCl wheel Windows validado aqui; G1 fixa versão/hash).

## Segurança e dados

Chave nunca persistida fora do DPAPI; AD vincula arquivo+índice; parcial pré-falha descartado pelo chamador; sem nomes/áudio real nos testes.

## Limitações

E1 DONE. Próxima: T-13.E2 (recuperação de sessão).
