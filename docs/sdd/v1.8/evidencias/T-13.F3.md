# T-13.F3 — índice e acessibilidade comportamental

- Estado: DONE
- Base: `62abbc0ba82014f001215bedea8b951996182e34`
- Resultado: commit de fechamento deste arquivo
- Requisito: `UX-13.F3`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; Node v22.22.0 (vitest + Chromium)
- Arquivos alterados: `indice_transcricoes.py` (novo, 206), `tests/test_indice_transcricoes.py` (novo), `assistente.py` (`/api/reunioes-indice`), `templates/assistente.html` (drawer + revisão), `static/assistente.js` (trap de foco, loading, revisão, preservação), `static/assistente.css` (drawer + foco), `tests/e2e/accessibility.spec.js` (novo), `docs/sdd/v1.8/evidencias/T-13.F3.md` (este)
- Decisões locais: índice JSON com snapshot manifests+sha+mtime (re-hash só se mtime mudou); primeira visão confirma, mudança marca `desatualizado` até `atualizar_indice`; cursor = id, limite 1–100; `title`/`duration` None (sem abrir conteúdo); drawer com trap de Tab, Esc, foco restaurado, `escapeHtml`; skills ausentes — ciclo literal.

## Baseline

Sem índice (scan + preview por listagem). Drawer sem trap/loading.

## RED

5 ausências (NotImplementedError) no índice; a11y sem cobertura.

## GREEN

Índice 6/6 (1000 sem `.txt`, staleness pós-job, cursor/limites, tamper→desatualizado, confirmação explícita, API paginada). E2E a11y 3/3 (foco circula/restaura + revisão visível; clipboard negado visível; filtro preserva + 375/860/1366 sem overflow).

## Regressão e gate

- Teste final F3: índice + api → verde; Python completa: **682 corpos, 0 falhas de código**; unit **12 passed**; e2e **16 passed** (1 regressão minha no drawer — loading sobrescrevia sucesso — corrigida).
- p95 medido: **0,5 s** p/ 100 itens em 1000 reuniões em cache (< 1 s).
- `compileall`/`diff-check`/`node --check` OK; limites OK.
- Ruído ambiental: worker real com heartbeat/5s derruba o guarda em teardown (só mtime; intocado por restrição); `test_isolamento` cai pelo mesmo motivo.
- Screenshots: DOM sintético verificado sem overflow; sem dados reais.

## Segurança e dados

Listagem sem preview de fala e sem decrypt; sem nomes reais; nada tocado.

## Limitações

F3 DONE. Próxima: T-13.G1 (dependências e CI).
