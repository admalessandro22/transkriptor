# T-13.D2 — transporte autenticado da extensão

- Estado: BLOCKED
- Base: `cf0c62b66734598af1f4820edb7f604ac1f22e7a`
- Resultado: `765ef1d8beb15fa6f3059037743a57be7aab8d74` (commit histórico deste arquivo) (parte automatizada; demo Chrome+Edge pendente)
- Requisito: `SEC-13.D2`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; Node v22.22.0; Chromium headless (Playwright)
- Arquivos alterados: `meet_bridge.py`, `config.py`, `transkriptor.pyw`, `extension/meet/background.js` (novo), `extension/meet/pairing.html` (novo), `extension/meet/pairing.js` (novo), `extension/meet/content.js`, `extension/meet/manifest.json`, `tests/test_meet_bridge_seguranca.py`, `tests/test_meet_bridge.py`, `tests/js/content.test.js`, `tests/js/background.test.js` (novo), `tests/e2e/meet-transport.spec.js` (novo), `tests/e2e/content-source.spec.js`, `docs/sdd/v1.8/evidencias/T-13.D2.md` (este)
- Decisões locais: `Origin` exata (`chrome-extension://[a-p]{16,32}` ou http 127.0.0.1/localhost; `None` reprova); `Pareador` (convite `pair-` uso único → credencial `sess-` por sessão, com reconexão e revogação); fallback legado documentado até D4; token bucket 20/s + rajada 40 com contador; `max_size` 16 KiB; máx. 4 conexões; content sem WS/token; skills Superpowers ausentes — ciclo literal.

## Baseline

Bridge/segurança verdes antes do RED; content.js com WS direto + token em `config.js` (arquivo gitignored desde sempre — nenhum segredo no repo; placeholder aplicado localmente).

## RED

9 casos novos em `test_meet_bridge_seguranca.py` → 6 falhas comportamentais (prefixo enganoso, Origin ausente, ID de extensão, `Pareador` inexistente, sem limite de rajada). Frame grande/sender maligno já valiam como travas.

## GREEN

`python -m pytest tests/test_meet_bridge.py tests/test_meet_bridge_seguranca.py` → **23 passed** (2 correções honestas: clientes de teste passam `Origin` como navegador real; rajada 45→100 pois o bucket 40+20/s absorve 45 em ~300 ms; `>=` em expiração 0 para determinismo; regex `{16,32}` preserva teste legado — scheme `chrome-extension://` já é inforjável por páginas).

## Regressão e gate

- Python completa: **593 passed**.
- `npm run test:unit` → **5 passed**; `npm run test:e2e` → **5 passed** (e2e legado adaptado ao novo canal, mesma asserção).
- `compileall` + `git diff --check` → exit 0. Sem segredos no diff (`config.js` gitignored; placeholder local).
- Demo handshake WS autenticado em **Chrome e Edge reais** (DU-12): **PENDENTE — NÃO EXECUTADO**. Um navegador não substitui o outro; sem desativar autenticação.

## Segurança e dados

Apenas localhost, fixtures sintéticas e portas de teste; nenhum dado real, conta ou perfil pessoal tocado.

## Limitações

Automatizado DONE; D2 BLOCKED exclusivamente pela demo nos dois navegadores. Checkbox desmarcada; D3 não começa.

## Remediação auditada em 23/09/2026

- A frase final acima é o checkpoint histórico daquele commit; D3–D7 e as correções das Tasks 2–6 foram executadas depois. Cliente e bridge novos estão nos commits `95045e456100ffa9cf1a5d816ce43b6d86eb4a87` e `362c63d2fc2b4c3ab4b7dd92e19833d907a843a7`, com RED/GREEN em `REMEDIACAO-2026-09-22.md`.
- `npm run test:unit` exit 0, 22 passed; `npm run test:e2e` exit 0, 21 passed no Step 1 atualizado da Task 12. A demo com extensão instalada em Chrome **e** Edge reais, pareamento descartável e reconexão segue PENDENTE. Estado atual: BLOCKED, sem inferir o gate real do Playwright.
