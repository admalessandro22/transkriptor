# T-13.E4 — eventos de log e ciclo de biometria

- Estado: DONE
- Base: `5a5c1427d1e5245577bf58c9cb4fae342bcf304d`
- Resultado: commit de fechamento deste arquivo
- Requisito: `SEC-13.E4`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1
- Arquivos alterados: `status_seguro.py`, `diagnostico.py`, `perfil_voz_flow.py`, `app_bandeja_menu.py`, `transkriptor.pyw`, `docs/MANUAL-USUARIO.md`, `tests/test_privacidade_eventos.py` (novo), `docs/sdd/v1.8/evidencias/T-13.E4.md` (este). `renomear_falante_flow.py` inalterado (independência já garantida em D7, coberta por teste).
- Decisões locais: prefixes legados intactos (incidente 08-07); API tipada ao lado (`emitir_evento` com allowlist, livre omitido, resto ValueError; `sanitizar_excecao` redacta); export de diagnóstico com allowlist estrutural (paths→`<pasta-pessoal>`, credenciais→`<credencial>`, detalhe de fonte omitido); opt-in com finalidade+consentimento (recusado sem, registrado com); `revogar_perfil_voz` com confirmação; política efetiva logada sem fala; manual com voz/retenção/responsável (sem alegar conformidade por checkbox); skills ausentes — ciclo literal.

## Baseline

Status/log/voz → 12 aprovados. Sem API tipada, export sanitizado ou opt-in.

## RED

7 ausências (ImportError/ TypeError) — lacunas reais de API.

## GREEN

→ exit code 0, **10 aprovados** (fala com prefixo omitida em evento; token fora da exceção; allowlist; export sem PII; opt-in recusado; finalidade/consentimento; correção sem biometria; revogação verificada; retenção sem auto-exclusão; canário fora de job/log).

## Regressão e gate

- Teste final E4: privacidade + status + log + voz → **22 aprovados**.
- Suíte completa: **666 passed**, guarda de estado real verde.
- `compileall` + `diff-check` → exit 0; limites OK.
- Gate real: não exigido (canário sintético; sem pessoas reais).

## Segurança e dados

Nenhum conteúdo/token/nome em log/diagnóstico/job por padrão; opt-in separado e revogável; nada real tocado.

## Limitações

E4 DONE. Próxima: T-13.F1 (histórico isolado por reunião).
