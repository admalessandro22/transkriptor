# T-13.A2 — gate de fala e testes JavaScript reais

- Estado: `DONE`
- Base: `2d7a521e4dc73f788231b3d5e706e4d067fd411e`
- Resultado: `604c93511f03a45fa6ffd4be6e0e56e764fd461f` (commit de implementação)
- Requisito: `NFR-13.A2`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; Node 22.22.0; Vitest
  5.0.1; Playwright 1.63.0 com Chromium de teste no cache local.
- Arquivos alterados: gate de reunião, fixtures pytest, testes de gate e
  `.gitignore`; `package.json`/lock, configuração Vitest/Playwright e testes
  JS/E2E.
- Decisões locais: a frase do gate é avaliada por assinatura lexical (≥75% dos
  termos distintivos e o termo `transkriptor`) para tolerar variação normal de
  STT sem aceitar cabeçalho, silêncio ou fala arbitrária.

## Baseline

`python -m pytest tests/test_log_isolado.py tests/test_isolamento_estado_local.py
-v --tb=short` → exit code 0, 4 testes aprovados.

## RED

`python -m pytest tests/test_gate_reuniao_qualidade.py -v --tb=short` → exit
code 1, 3 falhas comportamentais: o gate aceitava metadados do cabeçalho,
`(nenhuma fala reconhecida)` e frase diferente da locução de teste.

As fixtures novas também foram introduzidas com RED: 3 erros de fixture
ausente antes da implementação. O primeiro cenário Vitest revelou uma fixture
DOM imprecisa (texto colocado em atributo, enquanto o parser real lê
`textContent`); a reprodução em jsdom confirmou a causa. O primeiro E2E
chegou ao evento correto, mas o matcher exigia igualdade exata e rejeitava o
metadado legítimo `ts_ms`; a asserção foi reduzida aos campos contratuais.

## GREEN

- `python scripts/verificar_fase.py --fase v1.8-sdd --tarefa T-13.A2` → exit
  code 0, `RASTREABILIDADE OK`.
- `python -m pytest tests/test_gate_reuniao_qualidade.py
  tests/test_log_isolado.py tests/test_isolamento_estado_local.py -v
  --tb=short` → exit code 0, 10 testes aprovados.
- `npm run test:unit` → exit code 0, 1 arquivo/1 teste Vitest aprovado; o
  teste executa `extension/meet/content.js` real, sem espelho Python.
- `npm run test:e2e` → exit code 0, 1 cenário Playwright aprovado em contexto
  de navegador isolado; o manifest e o source da extensão são lidos do
  checkout, sem carregar `extension/meet/config.js`.

## Regressão e gate

- `python -m pytest tests/test_gate_reuniao_qualidade.py
  tests/test_log_isolado.py tests/test_isolamento_estado_local.py
  tests/test_gitignore_docs.py -v --tb=short` → exit code 0, 16 aprovados.
- `npm run test:unit` → exit code 0.
- `npm run test:e2e` → exit code 0; houve aviso externo de `NO_COLOR` versus
  `FORCE_COLOR` do executor, sem falha de teste.
- `git diff --check` → exit code 0 antes do commit de implementação.
- Gate físico de 25 s/600 s: **PENDENTE**. Não foi executado porque reproduz e
  captura áudio e requer autorização específica.

## Segurança e dados

Nenhum microfone, alto-falante, loopback, Whisper, reunião, conta Google ou
perfil pessoal de extensão foi usado. WAV/TXT, relógio e servidor TCP são
sintéticos/temporários; o servidor escuta somente `127.0.0.1` e encerra socket
e thread no teardown. Fixtures usam `Pessoa Sintética`; `config.js` local não
é lido pelo runner. `node_modules`, resultados e relatórios Playwright ficam
ignorados pelo Git.

## Limitações

O gate físico continua pendente e só comprovará a cadeia áudio→Whisper depois
de autorização própria. Esta tarefa não altera o transporte, pareamento,
armazenamento ou identificação de participantes do Meet; a próxima tarefa
elegível é `T-13.B1`.
