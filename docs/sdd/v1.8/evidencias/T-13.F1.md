# T-13.F1 — histórico isolado por reunião e limitado

- Estado: DONE
- Base: `4f528f68ec8680f96a08c078c81568e1db6ea47f`
- Resultado: commit de fechamento deste arquivo
- Requisito: `UX-13.F1`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1; Node v22.22.0 (vitest + Chromium)
- Arquivos alterados: `assistente_validacao.py` (`ChatRequest` + janela), `assistente.py` (usa `ChatRequest`), `static/assistente.js` (isolamento + geração + janela + input), `tests/test_assistente_contrato.py` (novo), `tests/test_token_sessao.py` (histórico longo: trim), `tests/e2e/chat-context.spec.js` (novo), `docs/sdd/v1.8/evidencias/T-13.F1.md` (este)
- Decisões locais: `meeting_id` default = transcrição (compat); `generation_id` default = uuid servidor; janela corta antigas (nunca rejeita a 12ª); troca/limpeza aborta + invalida por geração; DOM e histórico por reunião (restaura ao voltar); pergunta só limpa o input após validar; skills ausentes — ciclo literal.

## Baseline

API/segurança/token sem isolamento (histórico global). Sem contrato F1.

## RED

Contrato: trim ausente (24 itens rejeitados). E2E: sem `meeting_id`/`generation_id`, DOM cruzado, input apagado.

## GREEN

Contrato 5/5; e2e contexto 5/5 (12ª pergunta, A→B isolado, tardia descartada, limpar invalida, input preservado — payloads e DOM inspecionados).

## Regressão e gate

- Python completa: **671 passed** (7 fiações 500 corrigidas no caminho: `pedido.model/meeting_id`, `dict` guard, trim herda E3-estrito).
- Histórico longo legado atualizado para trim (F1 supersede E3-rejeita no HTTP; unit E3-estrito intacto).
- Unit **12 passed**; e2e **13 passed**; `compileall`/`diff-check`/`node --check` OK; limites OK.
- Gate real: não exigido (stub de fetch; Ollama mockado).

## Segurança e dados

Isolamento é por reunião selecionada; sem vazar conversa entre reuniões; limites validados.

## Limitações

F1 DONE. Próxima: T-13.F2 (orçamento e fundamentação).
