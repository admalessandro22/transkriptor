# T-13.D6 — atribuição conservadora e calibrada

- Estado: DONE
- Base: `7f12102cdddb56a5be572c09a5028d6d38451f3d`
- Resultado: commit de fechamento deste arquivo
- Requisito: `FR-13.D6`
- Ambiente: Windows; Python 3.12.10; pytest 9.1.1
- Arquivos alterados: `identidade_reuniao.py` (novo, 222 linhas), `tests/test_identidade_reuniao.py` (novo), `correlacionador.py`, `diarizador.py`, `config.py`, `tests/test_correlacionador.py` (7 casos ao novo contrato), `docs/sdd/v1.8/evidencias/T-13.D6.md` (este)
- Decisões locais: `Atribuicao(participant_id/display_name/source/score/evidence_ids/status)`; precedência manual → entry → legenda+tempo → voz → atividade (auxiliar); Jaccard legenda ≥ 0,5, empate ≤ 0,05 e homônimos sem id → CONFLICT/UNKNOWN; sobreposição e relógio >1,5 s nunca confirmam; `MODO_AUTO_NOMES = False` (só manual confirma; resto é sugestão p/ D7); `aplicar_nomes_meet` só aplica CONFIRMED; voz/mic só reivindica `FALANTE_XX`; `avaliar_atribuicoes` publica precisão/cobertura/abstinências; skills ausentes — ciclo literal.

## Baseline

`test_correlacionador + test_diarizacao_voce` → 23 aprovados (comportamento antigo). Resolvedor não existia.

## RED

`tests/test_identidade_reuniao.py` → 7 falhas: 6 `NotImplementedError` + 1 regressão real no código antigo (`aplicar_nomes_meet` trocava `VOCÊ` por voto de frequência — o defeito que D6 elimina).

## GREEN

→ exit code 0, **7 aprovados** (legenda fraca preserva VOCÊ; empate/homônimos/sobreposição abstêm-se; manual confirma; legenda forte sugere; relógio incerto rebaixa; métricas publicadas).

## Regressão e gate

- Teste final D6: identidade + correlacionador + diarizacao_voce → **29 aprovados**.
- 7 testes legados atualizados ao contrato FR-13.D6 (frequência e sobrescrita de VOCÊ/voz removidas — mudança especificada em `tasks.md`, não defeito mascarado; helpers `correlacionar_*`/`similaridade` intactos).
- Suíte completa: **610 passed**; `compileall` + `diff-check` → exit 0; limites de linhas OK.
- Gate real (corpus/precisão ≥98%): pendente de G3; até lá, sugestão revisável.

## Segurança e dados

Nomes sintéticos (Ana/Bruno); sem biometria, conta ou áudio real. Nada no diff além de lógica.

## Limitações

D6 DONE. Próxima: T-13.D7 (resultado canônico + correção/undo).
